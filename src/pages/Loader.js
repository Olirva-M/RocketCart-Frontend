import React from 'react';
import '../css/Loader.css'; // Ensure you have the appropriate CSS for the loader

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
